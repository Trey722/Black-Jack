package objects;
public class game {
    Player player; 
    Dealer dealer; 


    public int startGame() // 0 Means dealer win | 1 means user won
    {
        boolean x = true; 

        deck decks = deck(); 
        Player player = player();
        while (x == true)
        {

        }
        return 0; 
    }
}
